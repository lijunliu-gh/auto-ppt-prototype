# GitHub Setup Guide

This file collects the recommended GitHub repository metadata for publishing Auto PPT Prototype.

## Repository Name

Recommended repository name:

- `auto-ppt-prototype`

## Suggested Description

Recommended short description:

> AI-agent-ready PowerPoint backend: plan, revise, and render PPTX decks from natural-language prompts. MCP + CLI + HTTP interfaces.

Shorter variant:

> AI-agent-ready PowerPoint backend with source-aware deck generation.

## Copy-Paste GitHub About Text

Use this directly in the GitHub repository About field:

`AI-agent-ready PowerPoint backend: plan, revise, and render PPTX decks from natural-language prompts. MCP + CLI + HTTP interfaces.`

Use this if you want a shorter About field:

`AI-agent-ready PowerPoint backend with source-aware deck generation.`

## Copy-Paste Repository Intro

Use this in the top of the repository description, project page, or announcement post:

> Auto PPT Prototype is an experimental open-source backend for AI-agent-driven PowerPoint generation. It uses a Python planning layer for deck creation and revision, then a JavaScript renderer for final `.pptx` output through MCP, CLI, JSON skill, and HTTP integration modes.

## Suggested Topics

Recommended GitHub topics:

- `powerpoint`
- `pptx`
- `ai-agent`
- `mcp`
- `agent-workflow`
- `presentation-generation`
- `presentation-automation`
- `python`
- `nodejs`
- `document-processing`
- `automation`

Suggested minimal topic set if you want fewer tags:

- `powerpoint`
- `pptx`
- `ai-agent`
- `python`
- `nodejs`
- `presentation-generation`

## Suggested Website Field

Use one of these:

- project homepage if you have one
- documentation site if added later
- leave empty for now

## Suggested Social Preview Direction

Use a simple preview image that communicates:

- AI agent input
- trusted source material
- deck JSON planning
- PPTX output

Suggested overlay copy:

- title: `Auto PPT Prototype`
- subtitle: `Python planning layer + JavaScript PPTX renderer`

Avoid screenshots that imply fully autonomous or fact-guaranteed research.

## Suggested Pinned Feature Bullets

Use these in the repo body or release notes:

- Python planning layer for deck creation and revision
- MCP Server for Claude Desktop, Cursor, and Windsurf integration
- Chart data reliability with fallback validation
- JavaScript PPTX renderer retained as a stable output engine
- source-aware planning from files and URLs
- presenter-note source export with clean slides
- MCP, CLI, JSON skill, and HTTP integration modes

## Copy-Paste Release Summary

Use this short release summary when creating the latest GitHub release:

> v0.5.1 adds image asset pipeline (local image insertion, URL images, placeholder protocol) with full security, building on brand template engine (v0.5.0), chart reliability (v0.4.1), and MCP Server (v0.4.0).

## Suggested Repository Announcement Copy

Use this for a short social or team announcement:

> Auto PPT Prototype v0.5.1: AI-agent-ready PowerPoint backend with image pipeline, brand template engine, MCP Server, chart reliability, and multi-model LLM support. Insert images, use placeholders, or pass your .pptx template for brand-matched output.

## Suggested Repository Positioning

Recommended positioning:

- experimental
- agent backend
- source-aware presentation workflow

Avoid positioning it as:

- fully autonomous research agent
- guaranteed factual presentation generator
- production-ready hosted SaaS

## Suggested Next Public Tag

Recommended next tag:

- `v0.5.1`

Reason:

- Phase 4 (Image Pipeline) is the next planned milestone
- Phase 1 (MCP Server), Phase 2 (Chart Reliability), and Phase 3 (Brand Templates) are complete
- See ROADMAP.md for the full phased plan

## Suggested GitHub Release Title

Recommended title:

- `v0.5.1 — Image Asset Pipeline`